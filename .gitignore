# ============================================================================
# svgedit · SVG Beautifier   独立子项目的忽略规则
# ----------------------------------------------------------------------------
# 本目录是一个**独立 git 仓库**（自带 .git），规则只作用于此目录，
# 与上层 jev-zen 仓库的 .gitignore 互不影响。
# ============================================================================

# ── 依赖 ────────────────────────────────────────────────────────────────────
# playwright-core（端到端探针用）实际装在 .svgbuild/node_modules 下，
# 由下面那条 .svgbuild/ 一并忽略；这里的顶层规则留给 tools/ 的临时 npm 目录。
node_modules/

# ── Python ─────────────────────────────────────────────────────────────────
__pycache__/
*.py[cod]
*.egg-info/
.venv/
venv/

# ── 工作台：全部本地中间物（不入库，约 65 MB） ───────────────────────────────
# .svgbuild/ 是开发期工作台，内含：
#   · 60+ 个一次性诊断脚本（_mt.cjs / _ref_cmp.cjs / ui_probe.cjs …）
#   · 80 个探针输出 JSON（st3.json / sw_now*.json / edit2.json …）
#   · 26 张对照截图（crop_*.png / cmp_*.png）
#   · playwright-core 依赖
# 这些都可随时由 tests/ 与 tools/ **重建**，且体积远大于源码本身，故不入库。
# 需要复现历史数字时，report.md 已写明每个数字对应的命令。
.svgbuild/

# ── 探针输出 ───────────────────────────────────────────────────────────────
# tests/*.cjs 默认把结果打到 stdout；重定向时容易落在仓库根。
/*.probe.json
/*.probe.err

# ── 构建 / 补丁中间物 ──────────────────────────────────────────────────────
*.tmp
*.orig
*.rej

# ⚠️ 注意：**产物 svgb_beautifier.html 是要入库的**，不要在这里加忽略规则。
#    它是主交付物（双击即用的单文件产品页）：入库才能直接从 GitHub 下载/raw 打开，
#    也让「report.md 里的数字 ↔ 产物」这一对应关系可被 review。
#    唯一例外见 report.md §20：它是 build.py 从 ui.html 确定性生成的。

# ── 编辑器 / 操作系统 ──────────────────────────────────────────────────────
.DS_Store
._*
Thumbs.db
desktop.ini
*.swp
*.swo
*~
.idea/
.vscode/

# ── 日志与备份 ─────────────────────────────────────────────────────────────
*.log
*.bak
*.bak-*
*.corrupt.bak
