# 内置本体与规则围栏 · 实现报告 v1

> 任务：**构建内置本体和规则，完成 laya 决策的本体和规则围栏构建**
> 代码：`src/05b_ontology.js`（新增，755 行）+ `src/05_laya.js` / `src/06_geometry.js` / `ui.html` 接线
> 测试：`tests/ontology_fence.cjs`（33/33）
> 复跑环境：无头 Edge + `playwright-core`，`NODE_PATH=.svgbuild\node_modules`
>
> **本报告包含一个否定性结论，它比实现本身更重要**：实测表明「美化会破坏本图结构」这一前提
> 在**当前代码 + 目标文件**上不成立，因此围栏默认以**顾问模式**上线（只报告、不改动作集）。
> 依据见 §5。全部数字可复跑。

---

## 0. 结论速览

| 项 | 结果 | 证据 |
|---|---|---|
| 内置本体可用性 | ✅ 在 36 份语料 + 目标文件上零异常、零回归 | `sweep_final2` vs `baseline_h0`：`sumDelta 286.9 → 286.9`、`acc 52 → 52` |
| 本体复现实测缺陷的能力 | ✅ 三个关键量**逐位复现** | 越界 `253.71 = 156.83+96.88`、导引极差 `211.83`、锚点错位 `42.01` |
| 规则围栏（顾问模式） | ✅ 上线并已验证**完全无副作用** | 顾问/关闭两种配置分数与产物**逐字节相同**（`ontology_fence.cjs` G 组） |
| 规则围栏（强制模式） | ⚠️ 已实现、可一行开启，但**实测不该默认开启** | 语料 −22.35 分 / ≈0 不变量收益；目标文件 −7.40 分且**不变量全线变差** |
| 既有测试 | ✅ 全绿 | `smoke` 无失败、`edit_e2e` 47/47、`edit_cancel` 14/14 |
| 单文件产物 | ✅ 已重建 | 8 694 行 / 1 732 346 B |

**一句话**：本体与规则**建成了**且是准的；但它们**作为硬门**在当前实现下是负收益。
本报告把「为什么」量化到可以复跑，并把强制门留成 opt-in，等几何引擎补齐局部修复能力后再逐条定价开启。

---

## 1. 为什么需要这一层（问题陈述）

现有管线是「扁平 IR + 症状检测器 + 贪心 op + 分数门」。它能看见 40 个节点，但看不见：

* 「这 4 条 360×44 的横条是**同一列里的层叠**」→ 于是逐对独立地移动它们，左边缘从 211.83px 散开
* 「这 6 个 36px 的圆是**围绕中心的径向组**」→ 于是按两两碰撞处理
* 「这条箭头**锚在**那个方块上」→ 于是移动方块时不搬箭头

而分数门对结构是盲的：**一轮美化可以在 +8.4 分的同时把上述三件事都做坏**。
本层的职责是补上「结构事实」，并把这些事实表达成决策层能用的**动作集约束**。

---

## 2. 三条设计纪律（都是实测结论，不是偏好）

### ① 几何优先 —— 语义通道实测不可用

| 通道 | 可用性（10 个文件 / 720 个元素） | 结论 |
|---|---|---|
| `id` | 9 个手写原稿 **0 个 id**；目标文件 137 个 id **全是 `svg_\d+` 机生** | ❌ 不可用 |
| `class` | 仅 6/10 文件存在（`onto_platform_architecture` 与 3 个 `laya-*` 均为 0） | ❌ 不可靠 |
| XML 注释 | 原稿相邻率 100%，**经本工具保存后掉到 25%** | ❌ 会被自己摧毁 |
| **几何** | **10/10 文件、720/720 元素** | ✅ **唯一可用通道** |

→ 因此 `05b_ontology.js` **不读 `id`、不读 `class`、不读注释**，全部事实只从 `geomBox` 与画笔属性推出。

### ② 约束是布尔硬门，不是加权分

把结构量混进 `WEIGHTS` 会被其它维度增益抵消。实测那轮 +8.4 分里，**+5.4 分（64%）来自一个破坏性动作**
——把两条 1942 / 1957px 的装饰样条压成 `y=540` 的一条贯穿直线，同时让 5 条连线退化为零尺寸。
分数不但没拦住，还为此**奖励**了它。故本层只产出 `forbid / gate`，不产出分数。

### ③ fail-closed

置信度不足时**收窄动作集**，而不是照常输出 op。`conf < 0.5` 时禁止整体重排、不触碰 critical 项。

---

## 3. 本体结构（`Ontology.build`）

### 3.1 事实层

| 事实 | 判据 | 为什么这样定 |
|---|---|---|
| **装饰件（节点/边）** | `opacity/fill-opacity/stroke-opacity ≤ 0.3`，或 `fill=none` + 无标签 + 有描边 | 覆盖两条 0.22 的样条与 112px 描边圆环；**矩形类容器优先豁免**，否则卡片外框会被当装饰排除 |
| **退化路径** | `w<1 && h<1`（**两者都塌缩**） | 竖直连线 `w=0` 是合法的，只看 `w` 会误伤 |
| **容器框** | **矩形类**图元（`rect/polygon/polyline/image/use`）且含 ≥2 个其它节点中心 | 圆形是装饰环。实测 4 个同心装饰圆会「顺带包含」5 个节点中心，被误认成容器框并造出 **6 条假越界** |
| **归属** | 某节点中心落在多个容器内 → 取**面积最小**者；否则归「画布」 | **构造性保证唯一归属**，不依赖 `RegionGraph.members`（实测它 `invariant=false`、`sumMembers 51 > 40 节点`、`r10/r11` 包围盒全同、`r6/r7` 横跨两卡） |
| **附着** | 端点须**贴到节点边界**（`distToEdge ≤ 26px`），再按边定轴 | 见 §3.3 的两个真实缺陷 |

### 3.2 结构层

**容器越界**：节点中心在某容器内 ⇒ 其整框应在该容器内（留 12px 余量）。

**导引**：按作用域聚类 `l/r/cx` 与 `t/b/cy`，两级容差（已对齐 2.0px / 意图 `min(短边×0.12, 48)`）。
**但「断裂」只按模式判定**——实测按松容差聚类会在星状图上报出 **24 条假断裂**，而真正该报的
「4 条 bar 左边缘散开 211.83px」因散得太开聚不成类而**漏报**。

**模式**（作用域内 + 尺寸分档后扫描）：

| 模式 | 判据 | 实测教训 |
|---|---|---|
| 堆叠 | 同尺寸、纵向相接、水平带有重叠 | ① 全局按 y 排序会被**其它卡片的元素交错打断**（`stacks=0`）；② 不按**尺寸分档**会被两个 120×36 标签切成两段；③ 带重叠阈值取 50% 会认不出「已被打散 41%」的那一列 |
| 成行 | 镜像逻辑 | 目标文件卡片 3 正确识别为 3 元行，步距 240.5/241.49 → `cv≈0`，**因此正确判定为「无需修正」** |
| 径向 | **同尺寸**卫星 + 半径相对极差 ≤0.25 + 角度大致铺满 | 不加尺寸分档会出 **6 个垃圾径向组（成员 20 个、半径极差 366）**；加上后正好留下 6 个 36px 卫星 |

### 3.3 两个真实缺陷（写代码时踩出来的）

**缺陷 A：`IR` 的附着通道整体不可用。**
实测 `e.source` / `e.target` 在目标文件 13 条边的 26 个端点上**全部等于同一个节点** `n14`
——那是整幅背景 `rect (0,0,1920,1080)`。点在框内时到框距离恒为 0，`nearestNode` 永远选中它。
**因此 `e.attached` 恒真，附着信息为零。** 本体改为在排除背景/容器后自行求解。

**缺陷 B：只看「在框内」会把容器内部空处当成锚点。**
合成图里两支不到底的箭头下端落在卡片内部空处，被算成锚在卡片上，凭空多出 2 条 `err=0` 的假附着。
修正为要求**贴到边界**；这也是缺陷 A 的通用形态。

### 3.4 在目标文件上的复现（逐位对齐实测值）

| 不变量 | 本体值 | 独立实测值 | 一致 |
|---|---|---|---|
| 容器数 | 4 | 4 张卡片 `svg_10/48/85/106` | ✅ |
| 容器越界 | 2 条 / **253.71px** | `svg_113` 156.83 + `svg_109` 96.88 | ✅ **精确** |
| 导引极差 | **211.83px** | 平台栈左边缘 1065/853.17/1065/913.12 | ✅ **精确** |
| 锚点错位（最大） | **42.01px** | 卡片 3 箭头 x=752 vs 方块中心 709.99 | ✅ **精确** |
| 堆叠模式 | 1 个，成员 `n66,n68,n70,n72` | 平台栈 4 条 bar | ✅ |
| 径向组 | 1 个，6 成员，半径极差 27.8 | 星状图 6 个 r=18 卫星 | ✅ |
| 装饰件 | 2 节点 + 2 边 | 圆环 `svg_27/28`、样条 `svg_3/4` | ✅ |
| 退化路径 | 0（输入） | — | ✅ |

---

## 4. 规则围栏（`Ontology.fence`）

沿用既有架构：`05_laya.js` 原有的后置门禁（`should_relayout` 为假时从 `ranks` 摘掉 `global_relayout`）
就是同一形态。围栏**只改 `applied`、只收窄动作集，绝不新增动作**，因此几何引擎无需改动。

六条规则，全部**触发式**（不命中就不产生任何影响）：

| 规则 | 触发条件 | 效果 |
|---|---|---|
| `displace_cap` | 存在重叠对 | 公布 `d_max = max(1.5×重叠量, 0.06×画布短边)`；几何端强制执行 |
| `attach_guard` | 移动类策略的目标含带连接体的形状 | 禁止 `spacing\|*` 与 `overlap\|move_apart` |
| `deco_edge_guard` | `edge_crossing` 候选含装饰路径 | 禁止 `orthogonal_reroute` |
| `contain_guard` | 存在容器越界 | 禁止整体重排 / 画布重平衡，且不触碰 critical |
| `pattern_gap` | 模式领边散开或节奏不齐 | **仅报告**（现有策略集无法修复） |
| `low_conf` | 置信度 < 0.5 | fail-closed 收窄 |

**顾问 vs 强制**：`sopt.ontEnforce = true` 才施加 `forbid/gate`；默认只产出事实、规则命中与理由。
`LayaDecide.run` 返回值新增 `fence`，`gate` 新增 `dMax` / `ontology`，台账可审计。

---

## 5. ★ 否定性结论：为什么围栏默认不强制

### 5.1 语料定价（36 份样例，逐规则开关）

方法：每条规则**单独**开启（其余全关），对比基准（全关）。脚本 `.svgbuild/rule_price.cjs`。

| 规则 | 总分代价 | 不变量改善 |
|---|---|---|
| `displace_cap` | **0.00** | 无 |
| `attach_guard` | **−22.35** | `containPx` 6.5（单份样本） |
| `deco_edge_guard` | **0.00** | 无 |
| `contain_guard` | **0.00** | 无 |
| `pattern_gap` | 0.00 | 无 |
| `low_conf` | 0.00 | 无 |

→ **100% 的代价来自 `attach_guard`**，而它的收益是 1 份样本的 6.5px。

### 5.2 目标文件定价

**测量陷阱（差点得出错误结论）**：本页**第一次** `Pipeline.beautify` 与后续调用结果不同
——同一配置（`ontOff`）先后跑两次得到 `42.35 / 5 accepted` 与 `38.75 / 3 accepted`。
不预热就会把预热差异读成「配置的代价」。**本报告全部结论均在两次预热后取得。**

预热后（`.svgbuild/palantir_value.cjs`）：

| 配置 | 分数 | 代价 | 接受 | 不变量 vs 关闭 |
|---|---|---|---|---|
| 关闭围栏 ×2 | 38.75 / 38.75 | 0 | 3/22 | 同 |
| **顾问模式 ×2** | **38.75 / 38.75** | **0.00** | 3/22 | **完全同** ✅ |
| 仅位移上限 | 38.25 | −0.50 | 1/22 | ⚠️ 越界 `128→253.71`、导引 `183→211.83`、错位 `36→42.01`、错锚 `1→2` |
| 强制围栏 | 31.35 | −7.40 | **0/22** | ⚠️ 同上全线变差 |

**关键**：关闭围栏时，管线把输入修复得**比输入更好** ——
越界 `253.71→128`、导引 `211.83→183`、锚点错位 `42.01→36`。
而开启任何强制门后，**输出退回到输入的原值**（因为它把 3 个被接受的动作全挡掉了）。

### 5.3 结论

1. **「美化会破坏本图结构」在当前代码 + 本文件上不成立。** 管线净修复了这三个结构量。
   此前识别出的局部损伤（214px 位移、装饰样条塌陷）是**真实的局部效应，但被其它动作的净修复抵消**。
2. **全局收窄动作集是错误的修复手段**：它拦不住损伤的根因，却拦住了修复。
   正确的方向是**局部修复能力**——移动形状时跟随连接体（attachment-following）、
   几何感知的重路由——而不是一刀切地禁用整类策略。
3. 因此：**本体与规则上线（顾问模式，零副作用）；强制门保留为 opt-in**，
   必须先把几何引擎的局部修复补齐、再逐条定价开启。
   `displace_cap` 的几何端另有一处结构性问题需先解决：`separation()` 是**共享**设施
   （重叠推开 + reroute 家族取点两处调用点），一刀切收紧会连带影响重路由，须先拆分调用点。

### 5.4 仍然成立、值得保留的判断

* `displace_cap` 的**算术**是真实缺陷：为消除 **7.8px** 重叠，候选位移达
  `1482.8 − 1278 + 10 = 214.8px`（27 倍过度修正），因为正确的 y 向小位移因会撞第三个节点被过滤。
  规则本身有据，只是**生效范围**必须先收窄到重叠推开这一处。
* `deco_edge_guard` 拦下的那个动作（+5.4 分、64% 增益）确实是破坏性的：装饰样条被压直、5 条连线退化。
  它在语料上代价为 0，将来拆开调用点后可以放心开启。

---

## 6. 接线与验收

**接线**：`ui.html` 新增 `<script src="src/05b_ontology.js">`（`05_laya.js` 之后）→ 重跑 `python build.py`。
`IR.opts` 与 `sopt` 是**同一对象**，故 `sopt.ontOff / ontEnforce / ontOffRules / ontDisplaceCap` 都能到达本体。
本体是**软依赖**：未加载或构建抛错时围栏整体放行，绝不因本体报错而阻断修复。

**验收（全部复跑通过）**

| 测试 | 结果 |
|---|---|
| `tests/ontology_fence.cjs`（自带合成图，不依赖 gitignored 夹具） | **33/33** |
| `tests/smoke_svgb.cjs` | `failed: []` |
| `tests/edit_e2e.cjs` | **47/47** |
| `tests/edit_cancel.cjs` | **14/14** |
| 36 份语料扫描 vs HEAD 基线 | `sumDelta 286.9 → 286.9`、`meanAfter 93.517 → 93.517`、`acc 52 → 52` ✅ **零回归** |

> 36 份中仅 2 份在对照里出现差异，且差异**只在「被否决次数」计数**上：
> `dAfter = 0`、`dAcc = 0`，仅 `dRej = −1` 与 `−5`（合计 `rej 141 → 135`）。
> 该差异**先于本轮工作存在**——把本轮全部改动关掉（`ontOff` + 位移上限关闭）同样得到 135，
> 故与本体/围栏无关。**分数线与接受数在 36 份上逐位相同，无 crash。**

`tests/ontology_fence.cjs` 的 33 项断言分 8 组，覆盖：本体事实层（容器唯一归属、装饰件、
越界量 40px）、模式（堆叠 3 元、领边散开 30px、步距 60/60、不得凭空造行/径向）、
附着（只锚到方块、错位 0/15px、退化路径不得误判）、围栏顾问模式**无副作用**（不改 choice、
不裁 ranks、规则照常给理由）、围栏强制模式**确实收窄**（blocked 带合法顺位）、
端到端**顾问 ≡ 关闭**（分数与产物逐字节相同）。

---

## 7. 边界与已知限制

| # | 限制 | 等级 | 说明 |
|---|---|---|---|
| L1 | 强制门默认关闭 | **P0** | 见 §5；开启需先补齐局部修复并逐条定价 |
| L2 | `separation()` 调用点未拆分 | **P0** | `displace_cap` 无法安全启用（连带影响重路由） |
| L3 | `attach_guard` 触发面过宽 | **P0** | 只要图中有连接体就命中（语料 36/36 全命中），无法区分「有对齐可失去」与「无」 |
| L4 | 本体不产出任何坐标 | 设计约定 | 数值真相仍在几何引擎；本体只回答「做什么」 |
| L5 | 容器框限矩形类 | P1 | `<ellipse>`/`<path>` 面板图不会识别出容器 → `contain_guard` 对该类图不触发 |
| L6 | 模式识别对「打散程度」敏感 | P1 | 带重叠阈值取 15%；散得更开（<15%）仍认不出 |
| L7 | 径向组每作用域只取最好的一组 | P1 | 一个容器内两个同心径向组只报一个 |
| L8 | `RegionGraph` 的既有缺陷未修 | P1 | `invariant=false`、`sumMembers 51>40`、重复包围盒、`r6/r7` 跨卡；本体已**绕开**，但它仍在上游 |
| L9 | 语义通道不可用 | P2 | 本工具保存会摧毁注释相邻性（100%→25%），属既有行为 |

---

## 8. 复跑清单

```powershell
# 语法 + 构建
node --check src/05b_ontology.js
python build.py                      # → OK；产物 8 694 行 / 1 732 346 B

# 本体/围栏回归（33/33）
$env:NODE_PATH=".svgbuild\node_modules"; node tests\ontology_fence.cjs

# 既有功能测试
node tests/smoke_svgb.cjs ; node tests/edit_e2e.cjs ; node tests/edit_cancel.cjs

# 零回归证明（36 份语料）
cmd /c "set NODE_PATH=.svgbuild\node_modules&& node tests\sweep_rule.cjs > .svgbuild\sweep_final2.json"
node .svgbuild\cmp_sweep.cjs .svgbuild\baseline_h0.json .svgbuild\sweep_final2.json

# 逐规则定价 / 目标文件定价
node .svgbuild\rule_price.cjs
node .svgbuild\palantir_value.cjs    # 含两次预热；不预热会得到错误结论
node .svgbuild\ont_probe.cjs         # 本体在目标文件上的全部事实
```

**改动文件**：`src/05b_ontology.js`（新增）、`src/05_laya.js`、`src/06_geometry.js`、
`ui.html`、`README.md`、`tests/ontology_fence.cjs`（新增）、`svgb_beautifier.html`（重建）。
`.svgbuild/` 全部脚本与中间件为 gitignored 的临时件，不是交付物。
