# 固定区域内多元素相对位置布局 · 规则设计 v1

> 输入样本：`ontology-palantir.beautified.beautified.svg`（Palantir FDE 飞轮图，15 810 B / 197 行，sha256 `6c1d3f0c…`）
> 该文件已被本工具美化过**两轮**，因此它同时是"待修对象"和"现有规则的行为证据"。
> 所有数字均为对本文件（而非对话里粘贴的文本）的实测；粘贴文本与本文件**不一致**，见 §1.0。
> 复现脚本：`.svgbuild/palantir_dump.cjs`（全元素绝对几何转储）、`.svgbuild/palantir_cards.cjs`（卡片归属/占比复算）、
> `.svgbuild/palantir_attr.cjs`（美化前后逐元素对比 + 归因）、`.svgbuild/palantir_probe.cjs`（区域图/评分/幂等）。

---

## 0. 结论摘要

1. **用户 4 条 critique 里，3 条被实测证实、1 条不成立、1 条只对了一半**（§6 逐条给证据）。其中最严重的一条（第 4 块右倾/元素拥挤）**完全成立且可量化**：卡片 4 的 4 条平台 bar 里有 **2 条整条跑出了卡片**，左边缘极差 **211.83px**。
2. **这些缺陷是现有规则自己造成的，不是原始设计的问题。** 归因实验证明：一次美化让**分数 +8.4**（30.35 → 38.75），同时
   * 把平台栈左边缘极差从 **211.83 改成 266.1（更差）**；
   * 把卡片 3 的箭头错位从 **21.5 / 42.01px 改成 36 / 72px（翻倍）**；
   * 为消除一个 **7.8px** 的重叠，把一个卫星节点沿 x 推了 **214px**；
   * 把两条 **1942px / 1957px** 的装饰样条压成 **y=540 的一条贯穿直线**，并让 3 条连线退化为零尺寸。
3. 根因有 5 条结构性缺陷（§2），其中**最根本的一条是：打分门看不见结构破坏**。因此新规则的落地方式**不能只是加分项，必须把结构不变量写成 `checkOp` 的硬门**（§4.4）。
4. 规则体系按 **L0 结构模型 → L1 容器契约 → L2 导引 → L3 节奏 → L4 附着 → L5 均衡 → L6 径向 → L7 跨区域** 分层（§3），配套 6 个可测验收指标（§5）。
5. **诚实前提**：`spacing` / `style` 两个维度在本样本恒为 0，评分视野覆盖不到本次要修的东西（§7.1）。所以这套规则**不会**提高现有总分 —— 它的价值是**不再把图改坏**，以及为 F6 第二段定标提供指标。

---

## 1. 实测：这张图现在的真实状态

### 1.0 先更正一处输入：对话里粘贴的 SVG ≠ 附件文件

粘贴文本与附件在关键处不同，**不能用粘贴文本做分析**：

| 元素 | 粘贴文本 | 附件实测 |
|:--|:--|:--|
| `matrix(` 出现次数 | 8 | **2** |
| `rect#svg_10`（卡片 1） | `y="195"` | **`y="190"`** |
| `rect#svg_106`（卡片 4） | `x="1028"` | **`x="1010"`** |
| `rect#svg_96` | `x="380"` | **`x="358.5"`** |
| `rect#svg_101` | `x="642"` | **`x="599.99"`** |

另经 `Runtime.load` 前后逐元素对比（`.svgbuild/palantir_load_probe.cjs`）：**载入不改写几何** —— 文件属性 = 载入后属性 = `getCTM∘getBBox` 投影结果。所以后文一切数字都是文件里真实生效的值。

### 1.1 卡片网格其实是对齐的（用户建议 #1 已满足）

| 卡片 | 实测绝对框 | 尺寸 |
|:--|:--|:--|
| Card 1 `svg_10` | `(70, 190)` | 840 × 330 |
| Card 2 `svg_48` | `(1010, 190)` | 840 × 330 |
| Card 3 `svg_85` | `(70, 560)` | 840 × 330 |
| Card 4 `svg_106` | `(1010, 560)` | 840 × 330 |

列距恒为 **940**、行距恒为 **370**、尺寸全等。**"强制拉齐四个区块的底边和顶边"这条建议在本文件上已经成立**（要么是早先一轮美化已经做过，要么原始设计本就规整）。新规则里它只应当作为 **τ=0 时不动** 的护栏，而不是一条会持续改写坐标的规则 —— 否则每轮都会为浮点误差再动一次。

### 1.2 实测到的真实缺陷（按卡片）

成员归属判据：**中心点落在卡片内框**（跨界元素按中心归属，避免一个跨界元素污染两张卡）；只统计可见内容，排除满版底板与装饰样条。

| 卡片 | 成员 | 内容包围盒 | 占比 | 四边留白 l/r/t/b | 重心偏移 cx/cy |
|:--|--:|:--|--:|:--|:--|
| Card 1 | 21 | `(104, 201, 634, 249)` | **0.57** | **34 / 172.01** / 11 / 70 | **−69** / −29.5 |
| Card 2 | 52 | `(580.64, 201, 1255.41, 309)` | **1.40（溢出）** | **−429.36** / **13.95** / 11 / 10 | **−221.66** / 0.5 |
| Card 3 | 20 | `(103, 571, 773, 289)` | 0.81 | 33 / 34 / 11 / 30 | −0.5 / −9.5 |
| Card 4 | 27 | `(853.17, 571, 967.3, 273.57)` | 0.95 | **−156.83** / 29.53 / 11 / 45.43 | **−93.18** / −17.21 |

**逐条解读（这才是真正要修的）：**

* **Card 1 — 右侧死区。** 左留白 34px、右留白 **172.01px**（5 倍），内容重心左偏 **69px**。用户说的"大片空白"在**右侧**而不是下方（下方留白只有 70px）。可修，属 L5。
* **Card 2 — 星状图整体溢出卡片左侧 429px。** `g#svg_26` 起始于 `x=580.64`，而卡片 2 的左边界是 `1010` —— 它把 **429px 的内容画在卡片 1 的地盘上**；同时右留白只剩 **13.95px**（"应用 1"被挤到贴边）。这才是"星状图拥挤"的几何本质。**用户第 2 条成立。**
* **Card 3 — 包围盒其实很规整（留白 33/34，重心 −0.5）。** 它的问题**不在容器布局，而在连接体**（见下）。
* **Card 4 — 平台栈左侧溢出 156.83px，2 条 bar 整条在卡片外。** 用户第 4 条成立。

**Card 3 / Card 4 的具体几何（逐元素实测）：**

Card 3 的三个应用方块尺寸一致（220 × 94，`y=650`），间距 **20.5 / 21.49px**（差 0.99px，几乎可忽略）。但它们的下行箭头：

| 箭头 | 方块中心 x | 箭头 x | 错位 |
|:--|--:|--:|--:|
| `svg_94` | 228 | 228 | 0 |
| `svg_99` | **468.5** | **490** | **+21.5** |
| `svg_104` | **709.99** | **752** | **+42.01** |

**箭头 2、3 从错误的位置向下画** —— 用户第 3 条里"箭头与上方方块的垂直对齐度不够精确"**成立且可量化**。（"三个方块高度参差不齐"则不成立：全为 94。）

Card 4 的平台栈（`g#svg_108`，`translate(1245 748)`，4 条 360 × 44）：

| bar | 绝对 x 范围 | 本地左边缘 | 与卡片 4（1010..1850）的关系 |
|:--|:--|--:|:--|
| AIP | 1065.00 .. 1425.00 | −180 | 在内 |
| **Ontology** | **853.17 .. 1213.17** | **−391.83** | **左端超出卡片 156.83px** |
| Foundry | 1065.00 .. 1425.00 | −180 | 在内 |
| **Apollo** | **913.12 .. 1273.12** | **−331.88** | **左端超出卡片 96.88px** |

* 左边缘极差 **211.83px**（4 条本该左对齐的层叠 bar）。
* 纵向间距 **12 / 0 / 24px** —— 不等距，且 Ontology 与 Foundry **贴死（0px）**。
* 注意 `−331.88` / `−391.83` / `358.5` / `599.99` / `1778.47` / `1.22729` 这些**非整数坐标**：它们是自动化 op 写进去的指纹，不是作者的手写值。

**星状图的两条实测（用户第 2 条的两半，都成立，但落在不同子图上）：**

* 卫星节点（`g#svg_58` 下 6 个，全部 `r=18`，**半径一致 —— 这半不成立**）：到中心的距离 **132.0 / 136.3 / 142.4 / 144.8 / 147.2 / 152.0**（极差 20px），角度间隔 **46.2° / 52.6° / 53.3° / 66.1° / 70.0° / 71.8°**（6 个理想值应为 60°）。**"虚线长短不一、角度杂乱"完全成立。**
* Ontology 周围的 5 个节点（`g#svg_32` 下）：半径 **24 / 25 / 27 / 28 / 28** —— **"节点圆圈大小不统一"成立。**

### 1.3 归因实验：分数 +8.4 的同时把图改坏了

对附件跑一次 `Pipeline.beautify(strategy=rule)`：**30.35 → 38.75（Δ+8.4）**，接受 3 个批次、否决 11 个。逐元素前后对比：

| 被接受的 op | Δ分 | 它实际做了什么（实测） | 后果 |
|:--|--:|:--|:--|
| `spacing:distribute_weighted#10` | +0.8 | 把 10 处间距统一压向 **6px**；移动了 `svg_96/101` 等方块，**没有移动它们的箭头** | Card 3 箭头错位 **21.5 → 36**、**42.01 → 72（翻倍）** |
| `overlap:move_apart#4` | +1.2 | 为消除 **7.8px** 重叠把 `circle#svg_75` 沿 x 推 **214px**、`circle#svg_72` 推 **180px**（重叠量 7.8px） | 星状图卫星被甩出 200px 级距离；半径与角度分布被彻底打乱 |
| `edge_crossing:orthogonal_reroute#5` | +5.4 | 把 `path#svg_3`（1942px 样条）与 `path#svg_4`（1957px）改成 `(0, 540, 1920, 0)` —— **一条贯穿全宽的直线**；`svg_22` / `svg_77` / `svg_117` 退化为零尺寸 | 装饰波纹与连线束被摧毁；**本轮 64% 的增益来自这个破坏性 op** |

同一实验里的全局指标变化：

| 指标 | 美化前 | 美化后 | 判定 |
|:--|--:|--:|:--|
| 平台栈左边缘极差 | 211.83 | **266.1** | **更差** |
| Card 3 箭头最大错位 | 42.01 | **72** | **更差** |
| 退出所属卡片的元素数 | 0 | **4** | **更差** |
| 分数 | 30.35 | 38.75 | 更好 |

**这就是本次规则设计的全部理由**：分数在涨，图在坏。而**坏的方式恰好就是用户报的那几种**。

---

## 2. 现有规则为什么会做出这种事（5 条结构性缺陷）

| # | 缺陷 | 证据 | 后果 |
|:--|:--|:--|:--|
| **D1** | **逐对独立平移，没有"导引/组"概念。** `move_apart` 把重叠当两两问题，各自沿某轴推开 | 7.8px 重叠 → 214px 位移；平台栈 4 条 bar 本共享左边缘，被各自独立移动 | 破坏共享对齐；过度修正 27 倍；把元素甩出容器 |
| **D2** | **移动物体不搬连接体。** op 只改目标元素自己的 transform | `distribute_weighted` 移动 `svg_96/101` 但 `svg_94/99/104` 留在原地 | 箭头指向空处（21.5 → 36、42.01 → 72） |
| **D3** | **没有容器边界契约。** 没有任何规则声明"成员必须留在其所属区域内" | Card 2 内容溢出 429px、Card 4 两条 bar 整条在卡片外 | 内容画到别的卡片上；跨卡片视觉污染 |
| **D4** | **把设计意图当缺陷。** `fill="none"` 的装饰环、装饰样条被当作实体障碍/待优化路径 | 卫星被 112px 装饰环"压到" → 判为 overlap → 推 214px；两条装饰样条被"重路由"成直线 | 星状图被打散；装饰视觉被摧毁 |
| **D5** | **打分门看不见结构破坏。** `collision` / `edgeRouting` 改善就加分，`containViolations` / 导引离散 / 连接体错位 / 路径退化**都不在分数里** | +8.4 分的那一轮把上述 4 项全部改坏 | **规则体系缺一个"硬约束层"**；这是 D1–D4 能持续发生的根本原因 |

补充一条**区域层的实测缺陷**（D3 的上游）：

* `RegionGraph.build` 在本文件上产出 **18 个区域**，但 **`invariant: false`**、`sumMembers 51 > 40 节点` → 节点被重复归属；
* `r10` 与 `r11` 的包围盒**完全相同**（`1283.2, 209.2, 289.6, 301.6`，成员 7 与 6）；`r6`/`r7` 近似重复；
* `r6`/`r7` 的框是 `x 579.39 … 1511`，**横跨卡片 1 与卡片 2**；`r17` 横跨卡片 3 与 4 → **区域识别不尊重卡片容器**；
* 单成员卡片/框架区域的 `inkDensity` 恒为 **1.0**（一个铺满自身包围盒的矩形），使 `worstRegion = r4 (1.0)`、`balance 0.93` 成为**退化读数**；
* `hierarchy 0.67` / `tiePairs 36` 在本样本上数值异常，需单独复核；
* **命名通道 0/18**：4 个 `<!-- Card N -->` 注释在文件里**被聚在 `<g>` 之前**（实测源文件顶层顺序 = 载入后顺序：`title, desc, style, defs, 注释×4, g`），与任何元素都不相邻 → F5 **正确地拒绝猜测**。含义：**再美化产物上区域名不可恢复**，因为保存会把注释聚拢。

**结论：区域层必须先修好，才谈得上"在固定区域内布规则"** —— 否则规则会在错误的区域上生效（例如把卡片 1 与卡片 2 的成员算作同一区域的成员）。

---

## 3. 规则体系设计

分层原则：**上层的约束优先于下层，且上层是硬门（不参与打分）**；下层是"改善项"（参与打分）。

### L0 结构模型（前置，无动作）

四张表，全部从当前 DOM 现算，不依赖作者原文：

| 表 | 定义 | 容差 | 本样本实测用途 |
|:--|:--|:--|:--|
| **归属区域**（owner region） | 每个可见元素从内到外找**第一个**满足 `elem ⊂ region.inner` 的区域；都没有则归画布。**恰好一个**归属，修掉 `invariant:false` / 重复区域 / 跨卡片区域 | `inner` 内缩 `pad_min` | Card 2 的 52 个成员必须全归 Card 2，而不是归到横跨 C1–C2 的 r6/r7 |
| **导引集**（guides） | 在同一归属区域内，按轴统计成员的 `l / r / cx / t / b / cy`，容差 `τ_g`，**≥3 个成员的坐标聚类** 记为一条导引 | `τ_g = 2px` | 平台栈 4 条 bar 的 `l` 本应聚成一条导引（实测 −180/−180/−331.83/−391.88） |
| **附着图**（attachments） | ① 连接体：`path`/`line` 端点落在某形状边/中心 `τ_a` 内 → 附着；② 标签：`text` 位于形状内或紧邻 `τ_a` → 附着；③ **穿透单子 `<g>`** 求有效兄弟组 | `τ_a = 6px` | Card 3 的 `svg_94/99/104` 端点落在 `svg_91/96/101` 底边上 → 必须附着 |
| **径向组**（radial group） | ≥4 个成员围绕共同中心、半径相对极差 ≤ `τ_r`、角度近似均匀 → 径向组 | `τ_r = 25%` | `g#svg_54` 的 6 个卫星；`g#svg_32` 的 5 个节点 |

> L0 的第③项（穿透单子 `<g>`）是必需的：星状图的 6 个卫星各自被一个 `<g>` 包着，不穿透就根本看不见"它们是兄弟"。**这是当前规则完全缺失的能力**，也是"同级一致性"规则过去一直无法生效的原因。

### L1 容器边界契约（硬门）

* **触发**：成员绝对框超出其归属区域内框（内缩 `pad_min = 12px`）。
* **动作**：**先拉回，不扩容器** —— 沿越界轴的向内平移 `min(溢出量 + pad_min)`。只有当 `Σ成员外接 + 2·pad_min > 容器可用尺寸` 时，才允许**扩张容器**，且单轮扩张上限 `grow_cap = 8%`。
* **硬门**：`containViolations` **不得增加**。
* 依据：Card 2 溢出 429px、Card 4 溢出 156.83px 且 2 条 bar 完全在外、美化后又有 4 个元素改变归属卡片。

### L2 导引一致性（硬门 + 修复）

* **触发**：某导引组内出现离群者（`|coord − guide| > τ_out`）而组内仍有 ≥2 个成员共享该坐标。
* **动作**：**拉回离群者**，不移动多数派。（原则：**少数服从多数**，因为多数派更可能承载设计意图。）
* **硬门**：`guideSpreadMax` **不得增大**。
* 依据：平台栈左边缘极差 **211.83 → 266.1**（美化把它改坏了）；Card 3 三块间距 20.5 / 21.49。

### L3 节奏：等距 / 等尺寸（有收益才动）

* **等距**：同一行/列、同角色的兄弟，gap 取**中位数**；仅当 gap 的 `CV > 0.15` 或极差 > `8px` 时才重排为等距。
* **等尺寸**：同角色、同轴尺寸相对偏差 ≤ `τ_s = 8%` 时统一为中位数（不得导致容器溢出）。
* **反过度修正条款（重要）**：触发阈值必须**同时**满足 `相对 > 15%` 且 `绝对 > 8px`。
  * 依据：Card 3 的 20.5 vs 21.49（0.99px，CV≈2%）**不值得动**；卫星半径 132–152（极差 20px，CV≈5%）**也不值得**为了"完美"而大动 —— 真正该规整的是 **±46°～72° 的角度**（这是 33% 的偏差）与 Ontology 周围 5 节点 **24/25/27/28/28**（相对偏差 16.7%）。
* 依据：`distribute_weighted` 当前把一切间距无差别压向 6px，是典型的"为 1px 动全图"。

### L4 附着体跟随（硬门）

* **规则**：形状平移 Δ ⇒ 附着于它的**连接体端点平移 Δ**、附着**标签平移 Δ**；若连接体另一端附着于另一形状，则按新两端位置**重算路径**（保持两端锚点，不改变拓扑）。
* **硬门**：`attachedGapError` **不得增加**。
* 依据：`svg_94/99/104` 没跟着 `svg_91/96/101` 走，错位 21.5 → 36、42.01 → 72。

### L5 区域均衡与占比（只做安全的两种动作）

* **触发**：内容包围盒占用 < `fill_min`，**或** 四边留白极差比 `max/min > ratio_max`。
* **动作（仅此两种）**：
  1. **整体重定心**：把内容块作为整体平移，使重心回到区域中心 —— 不改任何元素尺寸、不改组内相对关系；
  2. **等比放大组内间距**：仅当区域是"展示型卡片"且占用 < `fill_min` 时，允许把组内间距乘 `s ≤ s_max`（默认 `1.25`）以填充，**元素尺寸不变**。
* **禁止**：为了填满而缩放元素、增删元素、改变语义顺序。
* 依据：Card 1 留白 34 vs 172.01（5 倍）、重心 −69px。
* 阈值建议：`fill_min = 0.45`、`ratio_max = 2.5`、`s_max = 1.25`。

### L6 径向规整（仅对径向组）

* **触发**：L0 识别出径向组，且角度 CV > `0.12` **或** 半径极差 > `20px`。
* **动作**：半径统一为**中位数**；角度均分为 `360/n`，**以第一个成员（按文档序）的原始角度为锚**（整体旋转保持不变，避免整组转向）；附着连线随之重绘。
* 依据：卫星半径 132.0–152.0、角度间隔 46.2°–71.8°（理想 60°）。

### L7 跨区域（护栏为主）

* 卡片/区块网格吸附：**仅当偏离 > 2px 才动**；本样本已对齐（列距 940、行距 370），因此这条规则在本图上应当是**零动作**。
* 配对区域（左右两列）的对称性：仅报告，不自动改。

---

## 4. 决策、仲裁与安全

### 4.1 优先级（冲突时高者胜）

`L1 容器契约 > L2 导引 > L4 附着 > L3 节奏 > L6 径向 > L5 均衡 > L7 跨区域`

理由：容器与导引是"结构真值"，被破坏后下层规则会在错误前提上继续修；附着是移动的**副作用**，必须与移动同时提交，不能事后补偿。

### 4.2 位移幅度上限（针对 D1 的过度修正）

* 轴选择：**优先沿缺陷的法向轴**（重叠沿分离轴、越界沿越界轴、错位沿对齐轴）。
* 幅度：`d = 恰好消除缺陷 + margin`，且 **`d ≤ d_max = max(1.5 × 缺陷量, 0.06 × 容器短边)`**；超限则**放弃该 op 并如实报告**，不得硬推。
* 依据：当前 7.8px 重叠 → 214px 位移（**27×**）。按 `d_max`，7.8px 重叠最多允许移动 `max(11.7, 0.06×330=19.8) = 19.8px`，214px 会被直接否决。

### 4.3 设计意图豁免（针对 D4）

* `fill="none"` 且 `stroke-width` 较大的**同心装饰环**：不作为实体障碍参与 overlap 判定；卫星落在环上不构成缺陷。
* `opacity < 0.3` 的**装饰样条**（本文件 `svg_3`/`svg_4`，`opacity=0.22`）：不参与 `edge_crossing` 重路由，也不作为障碍。
* 判据建议：`fill === 'none' || normColor(fill) === '' || opacity ≤ 0.3` → 标记 `decorative = true`，从障碍集与边集里排除。

### 4.4 结构不变量必须写成 `checkOp` 硬门（针对 D5，**最关键的一条**）

新增硬门（**不参与打分**，只做否决）：

| 硬门 | 定义 | 禁止的本次实测行为 |
|:--|:--|:--|
| `containViolations` | 超出归属区域的元素数 / 总溢出 px | 2 条 bar 被推出卡片 4 |
| `guideSpreadMax` | 各导引组坐标极差的最大值 | 平台栈 211.83 → 266.1 |
| `attachedGapError` | 连接体端点与其锚点形状边/中心的偏移 | Card 3 箭头 21.5 → 36、42.01 → 72 |
| `edgeDegeneracy` | 退化路径数（外接框面积为 0、或共线） | 3 条连线被压成零尺寸 |
| `decorativeDestroyed` | 被改写/删除的 `decorative=true` 元素数 | 2 条 1942/1957px 装饰样条被压成 y=540 直线 |

**规则**：一个 op 批次只有在**分数提升** 且**全部硬门不劣化**时才可接受。这一条比任何新打分项都重要 —— 因为它把"分数看不见的破坏"变成了一票否决。

### 4.5 幂等性

新规则须满足 `f(f(x)) = f(x)`（含浮点：坐标写入前按 0.5px 栅格取整，避免 `358.5` / `599.99` 这类累积漂移）。
**好消息**：当前管线的**分数幂等性已实测成立** —— 对美化输出再跑一次得 `38.75 → 38.75`，0 个 op 被接受。所以幂等风险在分数层已经排除，剩下的是坐标层的漂移。

---

## 5. 新增验收指标（全部可测，且本样本已有基线）

| 指标 | 定义 | 本样本实测基线 | 目标 |
|:--|:--|:--|:--|
| `containViolations` | 超出归属区域的元素数 / 总溢出 px | Card 2：1 组溢出 **429px**；Card 4：**2 条 bar 整条在外**；美化后另有 **4** 个元素改变归属 | 不增加（硬门）；理想为 0 |
| `guideSpreadMax` | 各导引组坐标极差最大值 | 平台栈 **211.83** | 单调不增；理想 ≤ `τ_g` |
| `attachedGapError` | 连接体端点与锚点形状中心/边的偏移 | Card 3：**0 / 21.5 / 42.01** | 单调不增；理想 0 |
| `contentOccupancy` | 内容包围盒 / 区域面积（或留白极差比） | Card 1 **0.57**（右留白 172）；Card 3 0.81；Card 4 0.95 | Card 1 ≥ 0.45 且留白比 ≤ 2.5 |
| `radialAngleCv` / `radialRadiusCv` | 径向组角度/半径离散 | 卫星角度间隔 **46.2–71.8°**（理想 60）、半径 **132–152** | 角度 CV ≤ 0.12、半径极差 ≤ 20px |
| `edgeDegeneracy` | 退化路径数 | 美化后 **+3** | 不增加（硬门） |

---

## 6. 与用户 4 条 critique 的逐条对照（含不成立项）

| 用户原文 | 实测判定 | 证据 | 归属规则 |
|:--|:--|:--|:--|
| **①** "内容区域在区块内占比不足 1/3，下半部分大片空白，头轻脚重" | **部分成立** | Card 1 占比 **0.57**，但空白主要在**右侧**（右 172.01 / 左 34，5 倍；重心左偏 69px），下方留白只有 70px | **L5** |
| **①** "箭头过于细小，缺乏线性推进感" | **未验证（属样式层）** | `stroke-width=3`；`style` 维度在本样本**恒为 0**，评分看不见 | L3（需先解决评分视野，§7.1） |
| **①** "下方文字说明与流程图脱节、排版较散" | **成立** | `svg_23`（生命周期说明）与 `svg_25`（需求结束…）依附于 `g#svg_12`，未被当作组的语义组成部分 | **L0 附着图 + L5** |
| **②** "星状图杂乱，节点圆圈大小不统一" | **成立（但不在卫星上）** | 6 个卫星全部 `r=18`（**一致**）；真正不一致的是 Ontology 周围 5 节点：**24 / 25 / 27 / 28 / 28** | **L3 等尺寸** |
| **②** "虚线长短不一，连接角度杂乱" | **成立** | 卫星半径 **132.0–152.0**（极差 20）；角度间隔 **46.2°–71.8°**（理想 60°） | **L6 径向规整** |
| **②** "应用 1 裸露在最右侧，没有独立容器包裹" | **前半成立、后半不成立** | 右留白仅 **13.95px**（贴边，成立）；但该元素**有**独立边框（`class="ok"` → `stroke:#39d98a; stroke-width:2`）。另外它的组带缩放矩阵 `matrix(1.22729 0 0 1.19623 1733.88 371.197)`，即被**放大 1.227× / 1.196×** —— 这是自动化写入的指纹，但**具体是哪条 op 写的尚未定位**（见 §7.4） | **L1 容器契约 + L5**（"没有容器"是观感，非事实） |
| **③** "顶部三个功能方块高度参差不齐" | **不成立** | 三块实测均为 **220 × 94**，`y=650` 齐平，间距 20.5 / 21.49 | —— |
| **③** "底部底座过于扁长" | **成立** | 底座 772 × 58，高度仅为方块的 62% | L3（但属设计取舍，建议**只报告不自动改**） |
| **③** "箭头与上方方块的垂直对齐度不够精确" | **成立且可量化** | 箭头 x = 228 / **490** / **752**，而方块中心 = 228 / **468.5** / **709.99** → 错位 **0 / 21.5 / 42.01px**；**美化后恶化到 36 / 72** | **L4 附着跟随（硬门）** |
| **④** "右侧严重失衡，左像一堵墙、右像一粒沙" | **成立** | Card 4 内容重心左偏 **93.18px**；4 条 bar 的左边缘极差 **211.83px**；平台栈左侧溢出 **156.83px** | **L2 导引 + L5 均衡** |
| **④** "左侧层级框间距过小、文字拥挤" | **成立** | 纵向间距 **12 / 0 / 24px**，Ontology 与 Foundry **贴死 0px** | **L3 等距** |
| **④** "元素重叠与拥挤，FDE 圆圈无法承接左侧信息" | **成立** | **2 / 4 条 bar 整条跑出卡片**（Ontology 853.17、Apollo 913.12，卡片左边界 1010）；FDE 组被 op 从 1605 推到 **1778.47（+173.47px）**，而其反馈箭头向左仅到 ~1418 | **L1 + L2 + L4** |
| **④** "回流动态感完全丧失" | **成立** | 三条反馈箭头 `svg_122/124/126` 的外接框右端在 1730 附近，而第三个标签 `svg_132` 在 **1286.62–1406.62** → 箭头与其标签脱节 | **L4** |
| 建议"统一区块高度、拉齐四区块顶底边" | **已满足** | 四卡实测 `(70/1010, 190/560)`，列距 940、行距 370、尺寸全等 | **L7 仅作护栏，本图零动作** |
| 建议"对关键产出增加半透明底色边框" | **不适用（属视觉设计，非几何）** | `class="ok"` 已有 `stroke:#39d98a`；加底色会改 `fill`，超出"调整相对位置"的范围 | —— 需产品决策 |

**统计**：14 条可判定项中，**成立 9 条、部分成立 2 条、不成立 2 条、不适用/未验证 2 条**（重叠计数见上表）。所有"成立"项都已落到具体规则层。

---

## 7. 诚实边界与未验证项

1. **这套规则不会提高现有总分。** `spacing` 与 `style` 在本样本恒为 0，评分视野覆盖不到本次要修的东西（`distribute_weighted` 之所以是"改善"，是因为它顺手压低了别的量）。真正的价值是 **§4.4 的硬门：不再把图改坏**。要让规则"有收益信号"，必须先做 F6 第二段定标。
2. **区域层必须先修（D3 上游）。** 在 `invariant:false`、区域重复、区域跨卡片的状态下，L1/L5 会在错误的区域上生效。**建议把"区域层修复"作为本规则集的第 0 步交付物**，且它可以用现有 `cmp_sweep.cjs` 的零回归判据验证。
3. **再美化产物上区域名不可恢复**（4 条 `<!-- Card N -->` 注释已被聚拢到 `<g>` 之前，实测源文件与载入后顺序一致）。这是 F5 命名通道的**固有边界**：它只在"作者原文"上有效。修复方向是**保存时把注释重新贴回其元素之前**，那是另一个改动。
4. **`move_apart` 的 214px 位移我还没读到产生它的那一行代码。** 本文档只证明了"行为与幅度"，尚未定位到"幅度是怎么算出来的"（可能是容器宽度或对侧边距被误用）。实现前必须先定位，否则 `d_max` 只是在外层兜住症状。
5. **径向组识别阈值（`τ_r = 25%`）是设计值，未在多样本上验证。** 本样本只有一个明确的径向组。
6. **`contentOccupancy` 的成员归属判据（中心点落入）会漏掉"大部分在外、中心在内"的元素。** 本样本用 `containViolations` 单独覆盖了这种情形，但两者需要合起来读。
7. **未验证**：把 `decorative` 豁免加进 `edge_crossing` 后，全量 36 样例的 `edgeRouting` 分布会如何变化（可能因为不再重路由装饰样条而丢掉一批"增益"，需要重新定标）。
8. **未验证**：`hierarchy 0.67` / `tiePairs 36` 是真实信号还是 F6 第一段的度量缺陷（我倾向于后者：本样本 `inkDensity` 已出现 `1.0` 退化读数）。
9. **本文档不改任何代码。** 落地顺序建议：`区域层修复 → L0 结构模型 → L4 附着跟随 → L1/L2 硬门 → §4.4 硬门进 checkOp → L3/L5/L6 → 阈值定标`。其中**前两步就能阻止本次实验中 3 个 op 里的 2 个继续破坏布局**。

---

## 8. 落地顺序与依赖

| 步骤 | 内容 | 依赖 | 验证方式 |
|:--|:--|:--|:--|
| **S0** | 区域层修复：唯一归属、去重复、禁止跨卡片 | — | `RegionGraph` 不变量成立；全量 37 样例 `invariant:true`；`cmp_sweep` 零回归 |
| **S1** | L0 结构模型（导引集 / 附着图 / 穿透单子 `<g>` / 径向组） | S0 | 本样本应识别出：平台栈 1 条左导引、Card 3 三对形状–连接体附着、2 个径向组 |
| **S2** | L4 附着跟随 + `attachedGapError` 硬门 | S1 | Card 3 箭头错位应保持 `0/21.5/42.01` 不再恶化 |
| **S3** | L1 / L2 硬门 + `d_max` 幅度上限 | S1 | 本样本 `containViolations` 与 `guideSpreadMax` 单调不增 |
| **S4** | §4.4 全部硬门进 `checkOp` | S2,S3 | 重跑 §1.3 实验：三个接受 op 应至少被否掉 `move_apart#4` 与 `orthogonal_reroute#5` |
| **S5** | L3 / L5 / L6 改善项 + 阈值定标 | S0–S4 | 全量 36 样例扫描（含新指标列）+ 定标 |
| **S6** | L7 护栏 + 坐标 0.5px 栅格化（幂等） | S5 | 连续两次美化：第二次 0 op 接受、坐标逐位不变 |

---

## 附：本次实测用到的复现命令

> ⚠️ 这些探针脚本目前放在 **`.svgbuild/`（已 gitignore 的临时目录）**，与被测样本 `.svgbuild/palantir.svg`（附件原样拷贝）在一起。
> 因此它们**不在 git 里**。如果希望把证据脚本随仓库提交（与 `tests/diag_batch.cjs`、`tests/sweep_rule.cjs` 同等对待），
> 需要把它们迁到 `tests/` 并把样本放到 `tests/fixtures/` —— 这是一个独立的整理动作，本文档未做。

```powershell
$env:NODE_PATH=".svgbuild\node_modules"

# 全元素绝对几何转储（154 行）→ .svgbuild/dump.json
node .svgbuild\palantir_dump.cjs

# 卡片归属 / 占比 / 留白 / 重心（本地复算，无浏览器）
node .svgbuild\palantir_cards.cjs

# 载入是否改写几何 + 注释在源文件与载入后的位置
node .svgbuild\palantir_load_probe.cjs

# 归因实验：美化前后逐元素对比 + 被接受 op 明细（写 .svgbuild/attr.json）
node .svgbuild\palantir_attr.cjs

# 区域图 / 评分 / 幂等
node .svgbuild\palantir_probe.cjs
```

> ⚠️ 本机 PowerShell 的 `>` / `*>` 重定向写出 **UTF-16LE**；`require('./x.json')` 会失败。上述脚本已改为用 `fs.writeFileSync(..., 'utf8')` 自写文件，或需经 `-Encoding Unicode` 读入后转存。
